var array = ["val1", "2", 2, 3, 4, false];
var array_car_brands = ["hyundai","Bugatti", "renault",
"Rolls Royce", "Tesla",
"Mercedes-Benz", "Ford"];
//Aplicar una o varias instrucciones a cada uno de los datos
/*array_car_brands.forEach(function(brand, cont, brands_array) {
  console.log(brand.toUpperCase());
});

array_car_brands.push("chevrolet");
console.log(array_car_brands);

array_car_brands = array_car_brands.filter(function(car_brand) {
  if(car_brand === "Tesla") {
    return false;
  }else {
    return true;
  }
});
console.log(array_car_brands);

array = array.filter(function(number) {
  if (typeof(number) === "number") {
      return true;
  }
  else {
    return false;
  }
});
console.log(array);

//Prototype remove
Array.prototype.remove = (function (index) {
  var result_array = this;
  result_array = array.filter(function(element, position){
    if (position === index) {
      return false;
    }
    else {
      return true;
    }
  });
  return result_array;
});
array = array.remove(2);
console.log(array);*/

 
